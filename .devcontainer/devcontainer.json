{
  "name": "BitChat Flutter Environment",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/java:1": {
      "version": "17"
    },
    "ghcr.io/devcontainers/features/android-sdk:0": {
      "version": "10406996",
      "buildToolsVersion": "34.0.0",
      "platformVersion": "34",
      "installSystemImages": true,
      "systemImageVersion": "google_apis;x86_64"
    }
  },
  // ★ CHANGE: 複雑なAVD作成コマンドを削除し、基本的なセットアップのみに簡素化
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y unzip libglu1-mesa && git clone https://github.com/flutter/flutter.git -b stable /usr/local/flutter",

  // ★ ADD: 環境変数を確実に設定
  "remoteEnv": {
    "PATH": "${containerEnv:PATH}:/usr/local/flutter/bin:/opt/android-sdk/emulator:/opt/android-sdk/platform-tools"
  },
  "remoteUser": "vscode",
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.flutter",
        "Dart-Code.dart-code"
      ]
    }
  },
  "forwardPorts": [
    6080
  ],
  "runArgs": [
    "--privileged"
  ]
}
